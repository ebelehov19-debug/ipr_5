apiVersion: v1
kind: ConfigMap
metadata:
  name: app-config
  namespace: todo-app
data:
  DB_HOST: "postgres-service"
  DB_PORT: "5432"
  DB_NAME: "appdb"
  REDIS_HOST: "redis-service"
  REDIS_PORT: "6379"
  PORT: "3000"
  NODE_ENV: "production"
  REACT_APP_API_URL: "/api"